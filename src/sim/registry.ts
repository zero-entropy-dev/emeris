/**
 * Merged identity table — how Identity acts, not a sixth core name.
 * One active world: meadow identities only.
 */
import { meadowIdentities } from "./identity";

export const identities = { ...meadowIdentities };
