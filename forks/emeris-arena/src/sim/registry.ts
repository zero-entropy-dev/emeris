/**
 * Merged identity table — how Identity acts, not a sixth core name.
 * One active world: arena identities only.
 */
import { arenaIdentities } from "./identity";

export const identities = { ...arenaIdentities };
